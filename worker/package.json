{
  "name": "flight-tracker-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for flight tracking",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.114.15"
  }
}
